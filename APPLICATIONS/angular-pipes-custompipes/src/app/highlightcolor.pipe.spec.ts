import { HighlightcolorPipe } from './highlightcolor.pipe';

describe('HighlightcolorPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightcolorPipe();
    expect(pipe).toBeTruthy();
  });
});
